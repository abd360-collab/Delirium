services:
  rabbitmq:
    image: rabbitmq:4-management
    container_name: delirium-rabbitmq
    restart: unless-stopped
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: delirium
      RABBITMQ_DEFAULT_PASS: delirium_dev_password